<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jayslog.springtest.mybatis.repository.RealEstateRepository">
    <select id="selectRealEstateId" resultType="com.jayslog.springtest.mybatis.entity.RealEstate" parameterType="int">
        SELECT
            `id`
            , `realtor_id`
            , `address`
            , `area`
            , `type`
            , `price`
            , `rent_price`
            , `created_at`
            , `updated_at`
        FROM
            `real_estate`
        WHERE
            `id`=#{id}
    </select>
    <select id="selectRealEstateRent" resultType="com.jayslog.springtest.mybatis.entity.RealEstate" parameterType="int">
        SELECT
            `id`
            , `realtor_id`
            , `address`
            , `area`
            , `type`
            , `price`
            , `rent_price`
            , `created_at`
            , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[`rent_price` <= #{rentPrice} ]]>
    </select>
    <select id="selectRealEstateListByAreaAndPrice" resultType="com.jayslog.springtest.mybatis.entity.RealEstate" parameterType="map">
        SELECT
            `id`
             , `realtor_id`
             , `address`
             , `area`
             , `type`
             , `price`
             , `rent_price`
             , `created_at`
             , `updated_at`
        FROM
            `real_estate`
        WHERE
            <![CDATA[
            `area` >= #{area}
            AND
            `price` <= #{price}
            ]]>
    </select>
    <insert id="insertRealEstateByObject"
            parameterType="com.jayslog.springtest.mybatis.entity.RealEstate">
        INSERT INTO `real_estate`
            ( `realtor_id`
              , `address`
              , `area`
              , `type`
              , `price`
              , `rent_price`
            )
        VALUE
        (
            #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
            , #{rentPrice}
        )
    </insert>
    <insert id="insertRealEstateByParameter" parameterType="map">
        INSERT INTO `real_estate`
        (
            `realtor_id`
            , `address`
            , `area`
            , `type`
            , `price`
            , `rent_price`
        )
        VALUE
        (
            #{realtorId}
            , #{address}
            , #{area}
            , #{type}
            , #{price}
            , #{rentPrice}
        )
    </insert>

    <update id="updateRealEstate" parameterType="map">
        UPDATE
            `real_estate`
        SET
            `type` = #{type}
            , `price` = #{price}
            , `updated_at` = NOW()
        WHERE
            `id`= #{id}
    </update>
    <delete id="deleteRealEstate" parameterType="int">
        DELETE FROM
                   `real_estate`
        WHERE
            `id` = #{id}
    </delete>
</mapper>